sudo: required
services:
  - docker

before_install:
  - docker build -t sungjaejung1031/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run sungjaejung1031/react-test npm test -- --coverage

after_sucess:
  - docker build -t sungjaejung1031/multi-client ./client
  - docker build -t sungjaejung1031/multi-nginx ./nginx
  - docker build -t sungjaejung1031/multi-server ./server
  - docker build -t sungjaejung1031/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sungjaejung1031/multi-client
  - docker push sungjaejung1031/multi-nginx
  - docker push sungjaejung1031/multi-server
  - docker push sungjaejung1031/multi-worker
